# API base URL. For local dev leave UNSET to use Vite proxy (requests go to same origin, proxy forwards to backend).
# Set only for production or when not using Vite dev server (e.g. VITE_API_URL=https://your-api.com/api)
# VITE_API_URL=http://localhost:3000/api
