# Database - Use your MongoDB connection string (local or cloud).
# Local:   MONGODB_URI=mongodb://localhost:27017/gym-saas
# Atlas:   MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/gym-saas?retryWrites=true&w=majority
#         (Get the URI from MongoDB Atlas: Cluster → Connect → Connect your application)
MONGODB_URI=

# MongoDB Atlas (optional) - For loading gym_users from external Atlas cluster.
# Include database name in URI, e.g. ...mongodb.net/rpesanddips
MONGO_URI=

# CORS (production) - comma-separated allowed origins, e.g. https://yourapp.vercel.app
CORS_ORIGIN=

# Frontend base URL (for QR check-in link; e.g. https://yourapp.vercel.app)
FRONTEND_URL=

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Twilio (optional - for SMS backup/defaulter notifications)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Telegram (optional fallback if tenant has no bot in DB)
TELEGRAM_BOT_TOKEN=
TELEGRAM_OWNER_CHAT_ID=
# Public API URL for registering per-tenant webhooks (e.g. https://gym-saas-api.onrender.com or ngrok: https://xxx.ngrok-free.dev). Required when tenant sets Telegram bot token.
PUBLIC_API_URL=

# Face recognition fallback (optional). When set, face check-in uses Python service for matching (see python-face-service/).
# Local testing: FACE_SERVICE_URL=http://localhost:8000 (run: cd python-face-service && uvicorn main:app --host 0.0.0.0 --port 8000)
# Production:   FACE_SERVICE_URL=https://your-python-face-service.onrender.com
FACE_SERVICE_URL=

# Web Push (optional - for PWA push notifications). Do NOT use simple values like 123456789.
# Generate a valid key pair with: npm run generate-vapid-keys
# Then paste the two lines below into .env (and in production env e.g. Render).
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# OpenAI (for Nutrition AI / calorie chat)
# Get your key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Cloudinary (for medical document uploads; required if members upload documents)
# Single URL format: cloudinary://API_KEY:API_SECRET@CLOUD_NAME
CLOUDINARY_URL=
# Or set separately:
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
